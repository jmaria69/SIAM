fastapi==0.115.0
uvicorn[standard]==0.30.5
pydantic==2.8.2
requests==2.32.3
python-dotenv==1.0.1
pydantic-settings==2.8.1
sqlalchemy==2.0.35
python-multipart==0.0.32  # Form() del panel señuelo de Active Defense (siem/router/honeypot.py)
pyotp==2.9.0

# Motor de IA plegable (módulos 2 y 9). Solo se usan si AI_PROVIDER lo
# requiere; el sistema funciona sin ellas con AI_PROVIDER=none (ver
# siam/ai/providers.py). "openai" también sirve para AI_PROVIDER=local
# (endpoints compatibles con la API de OpenAI, p.ej. Ollama).
anthropic==0.39.0
openai==1.54.0

# Tests
pytest==8.3.3
httpx==0.27.2
